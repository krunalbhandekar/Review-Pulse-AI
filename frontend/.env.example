# ----------------------------------------------------------------------
# Multi-Tenant Review Intelligence — Frontend
# ----------------------------------------------------------------------
# Only one runtime value: where the FastAPI server lives.
# Everything else (brand name, default lookback weeks, etc.) lives in
# code constants. See lib/config.ts.
# ----------------------------------------------------------------------

# Public base URL of the multi-tenant server (FastAPI).
# Used for OAuth redirects + the typed API client.
NEXT_PUBLIC_API_URL=http://localhost:8000
